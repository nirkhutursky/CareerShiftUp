name: job_seeker
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.6.0  # Replace with the latest version
  firebase_auth: ^5.3.1   # Replace with the latest version
  cloud_firestore: ^5.4.4  # Replace with the latest version
  google_sign_in: ^6.2.1  # Replace with the latest version


  provider: ^6.1.2 # Add the latest version


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
